name: caspian_college_messenger
description: "Messenger; Chat app; Caspian College"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  image_picker: ^1.1.2
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  file_picker: ^8.1.2
  flutter_contacts: ^1.1.9
  permission_handler: ^11.3.0
  media_kit: ^1.1.11
  media_kit_video: ^1.1.11
  media_kit_libs_windows_video: ^1.0.10
  media_kit_libs_linux: ^1.1.3
  media_kit_libs_macos_video: ^1.1.4
  media_kit_libs_ios_video: ^1.1.4
  media_kit_libs_android_video: ^1.3.6

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.0
  http: ^1.2.1
  web_socket_channel: ^3.0.1
  signalr_netcore: ^1.4.4
  path_provider: ^2.1.4
  open_filex: ^4.5.0
  share_plus: ^10.0.0
  video_player: ^2.9.2
  flutter_webrtc: ^1.4.0
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.4
  record: ^5.2.0
  just_audio: ^0.9.40
  just_audio_media_kit: ^2.0.0   # Windows/Linux backend для just_audio через media_kit
  audio_session: ^0.1.25         # инициализация аудио-фокуса для just_audio
  http_parser: ^4.0.0            # MediaType при загрузке аудио
  flutter_local_notifications: ^17.2.4  # локальные уведомления (Android/iOS/macOS/Windows/Linux)
  window_manager: ^0.3.9         # управление окном (иконка, заголовок) на Windows/macOS/Linux
  cryptography: ^2.7.0           # X25519 ECDH + AES-256-GCM + HKDF (шифрование сообщений)
  emoji_picker_flutter: ^4.4.0   # эмодзи-клавиатура (эмодзи + стикеры)
  cached_network_image: ^3.4.1   # кэширование сетевых изображений (GIF, аватары)

dependency_overrides:
  # record 5.2.x pulls record_platform_interface 1.5.0 but constrains
  # record_linux to ^0.7.0, which is incompatible with that interface version.
  # Force the newer linux implementation to fix Windows/macOS/Linux builds.
  record_linux: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# ── Конфигурация иконок приложения ───────────────────────────────────────────
# Источник: assets/images/logo.png (один файл → все платформы).
# После добавления logo.png запусти:  dart run flutter_launcher_icons
flutter_launcher_icons:
  image_path: "assets/images/logo.png"
  # Android (adaptive icon с белым фоном)
  android: true
  min_sdk_android: 21
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/logo.png"
  # iOS (без прозрачности — App Store не принимает)
  ios: true
  remove_alpha_ios: true
  # Web (favicon + PWA-иконки)
  web:
    generate: true
    image_path: "assets/images/logo.png"
    background_color: "#FFFFFF"
    theme_color: "#E05C38"
  # Windows (заменяет windows/runner/resources/app_icon.ico)
  windows:
    generate: true
    image_path: "assets/images/logo.png"
    icon_size: 256
  # macOS
  macos:
    generate: true
    image_path: "assets/images/logo.png"
